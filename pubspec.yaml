name: retrofit_graphql
description: |
  retrofit_graphql or Retrofit GraphQL for Dart is a powerful code generation tool that simplifies working with GraphQL in Dart projects.
version: 3.1.0
repository: https://github.com/Oualitsen/Retrofi-Graphql-For-Dart

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  petitparser: ^6.0.1
  source_gen: ^1.5.0
  glob: ^2.1.2
  logger: ^2.5.0
  build: ^2.4.1
  yaml: ^3.1.3

dev_dependencies:
  test:
  flutter_lints: ^3.0.1
  build_runner: 
