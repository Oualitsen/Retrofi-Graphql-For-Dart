# Retroft Graphql for dart

This is a class generator for parsing data retruned by a graphql call.
It generates input classes and type classes by parsing your schema, queries, mutations and subscritions.

